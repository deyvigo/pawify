meta {
  name: login users
  type: http
  seq: 2
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "useradmin",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
