meta {
  name: recovery password
  type: http
  seq: 2
}

post {
  url: {{actual_url}}/auth/recovery/request-code
  body: json
  auth: inherit
}

body:json {
  {
    "username": "userbuyer2"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
