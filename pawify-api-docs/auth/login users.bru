meta {
  name: login users
  type: http
  seq: 2
}

post {
  url: {{url_local}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "userbuyer1",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
