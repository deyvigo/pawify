meta {
  name: login users
  type: http
  seq: 2
}

post {
  url: {{actual_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "userbuyer2",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
