meta {
  name: reset password
  type: http
  seq: 11
}

post {
  url: {{url}}/auth/recovery/reset-password
  body: json
  auth: inherit
}

body:json {
  {
    "username": "useradmin",
    "code": "t7a9az",
    "new_password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
